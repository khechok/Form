window.addEventListener('load', () => {
   const params = (new URL(document.locaation)).searchParmas;
   const name = params.get('name');
   const email = params.get('email');

   document.getElementById
}
)